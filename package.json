{
  "name": "idb-range",
  "description": "MongoDB inspired interface for IDBKeyRange",
  "repository": "git://github.com/treojs/idb-range.git",
  "version": "3.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "keywords": [
    "indexeddb",
    "idbkeyrange",
    "range",
    "treo"
  ],
  "scripts": {
    "prepublish": "babel src --out-dir lib --presets es2015",
    "test": "eslint src/ test/ && browserify-test --transform [ babelify --presets es2015 ]",
    "development": "browserify-test --transform [ babelify --presets es2015 ] --watch"
  },
  "dependencies": {
    "is-plain-obj": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.1.18",
    "babel-core": "^6.1.20",
    "babel-eslint": "^4.1.5",
    "babel-preset-es2015": "^6.1.18",
    "browserify-test": "^2.1.2",
    "chai": "^3.4.1",
    "eslint": "^1.9.0",
    "eslint-config-airbnb": "^1.0.0",
    "treo-websql": "^2.0.0"
  }
}
